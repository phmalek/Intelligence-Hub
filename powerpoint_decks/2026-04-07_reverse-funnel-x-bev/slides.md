# Reverse Funnel x BEV

## Slide 1

### Layout Description

16:9 hero slide on deep graphite. The headline sits top-left with a short sub-headline below it. Center stage is a vertical system diagram of five rounded boxes, linked by precise arrows from top to bottom. At the base, the three channels sit as horizontally aligned pills. A thin electric-blue feedback loop returns from the channel row back to the sales target box, making the system visibly closed-loop. On the right, a subtle diminishing-returns curve signals saturation. Between channel pills, short bidirectional arrows indicate dynamic reallocation. A compact closing line sits low-left.

### Exact Text Content

Headline:
`FROM MEDIA PLANNING TO OUTCOME ENGINEERING`

Sub-headline:
`Reverse Funnel turns demand into a controllable system`

System boxes:
- `SALES TARGET`
- `REQUIRED LEADS (DCFS)`
- `SESSIONS`
- `MEDIA SPEND`
- `CHANNELS`

Channel labels:
- `SEARCH`
- `INVENTORY`
- `SOCIAL`

Overlay labels:
- `Observed conversion`
- `Saturation curve`
- `Dynamic reallocation`
- `Closed-loop optimisation`

Bottom line:
`Every EUR is traceable to sales impact`

### ASCII Wireframe

```text
FROM MEDIA PLANNING TO OUTCOME ENGINEERING
Reverse Funnel turns demand into a controllable system

  [ SALES TARGET ]  <-------------------------------+
         |                                          |
  [ REQUIRED LEADS (DCFS) ]                         |
         |                                          |
     [ SESSIONS ]                     saturation    |
         |                              curve       |
    [ MEDIA SPEND ]                                  closed-loop
         |                                          optimisation
      [ CHANNELS ]                                  |
   [SEARCH] <-> [INVENTORY] <-> [SOCIAL] ----------+

Every EUR is traceable to sales impact
```

## Slide 2

### Layout Description

Deep graphite background with a left-aligned title and a disciplined two-column grid beneath it. Five content blocks stack in the left two-thirds, each with a compact uppercase title and one sharp sentence. The right third contains a minimal marginal-returns chart and three horizontal channel bars showing budget shifting toward the strongest return. The chart and bars use the same electric-blue accent as slide 1.

### Exact Text Content

Title:
`REVERSE FUNNEL OPERATING LOGIC`

Blocks:
1. `START FROM SALES`
   `Budget derived from targets, not guesses`
2. `FIND THE GAP`
   `Quantify inefficiencies across the funnel`
3. `REALLOCATE`
   `Shift spend to highest marginal return`
4. `SCALE SYSTEM`
   `Codified learnings across markets`
5. `OUTPUT`
   `Always-on decision engine`

Chart labels:
- `Marginal return`
- `Spend`
- `Headroom`

Bar labels:
- `SEARCH`
- `INVENTORY`
- `SOCIAL`

Small caption:
`Reallocate before channels saturate`
